<%= form_for resource, :html => { :multipart => true } do |f| %>
<%= render 'shared/error_messages', :object => f.object %>
  <div class="field">
	<%= f.file_field :upload %>
	 <%= f.submit :Upload %>
  </div>
<% end %>
