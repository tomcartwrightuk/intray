<div class="" id="add_note">
	<% form_for(@resource) do |f| %>
		<div class="">
			<%= f.text_field :note, { :title => "Add a note to the file" } %></div>
			<div class="actions">
			<%= f.submit "Add note", :name => nil, :id => "add_note_button" %>
		</div>
	<% end %>
</div>