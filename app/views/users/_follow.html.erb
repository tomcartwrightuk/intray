<%= form_for current_user.relationships.build(:followed_id => @user_to_follow.id) do |f| %>
  <div><%= f.hidden_field :followed_id %></div>
  <div class="actions"><%= f.submit "Connect" %></div>
<% end %>